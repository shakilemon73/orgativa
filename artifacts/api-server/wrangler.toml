# Cloudflare Worker — Orgativa API
name = "orgativa-api"
main = "src/worker.ts"
compatibility_date = "2024-09-23"

# Uncomment and set if you need environment variables / secrets:
# [vars]
# MY_VAR = "value"
#
# Secrets are added via:
#   wrangler secret put SECRET_NAME
