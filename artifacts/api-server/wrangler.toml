name = "orgativa"
main = "src/worker.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

[vars]
# Add your environment variables here (non-secret)
# Secrets should be set via: wrangler secret put SECRET_NAME
# Required secrets:
#   DATABASE_URL (if Worker needs DB access)

[dev]
port = 8787
local_protocol = "http"
